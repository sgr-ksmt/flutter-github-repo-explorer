import 'package:gap/gap.dart';
import 'package:github_repo_explorer/widgets/space.dart';

class Gaps {
  const Gaps._();

  static const xxsmall = Gap(Space.xxsmall);
  static const xsmall = Gap(Space.xsmall);
  static const small = Gap(Space.small);
  static const medium = Gap(Space.medium);
}
